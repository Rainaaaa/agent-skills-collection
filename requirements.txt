# Pinned to the major versions tested in the AgentSkillsOSS conda env.
# Stage 1 (metadata_collection) needs Playwright + Chromium.
# Stages 2 + 3 (skills_repos) only need the standard library + requests.
# pandas + openpyxl are used by merge_metadata.py for SOC hierarchy joins.

requests>=2.31,<3
pandas>=2.0,<3
openpyxl>=3.1,<4
playwright>=1.45,<2
